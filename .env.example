# Em `npm run dev`, deixe vazio (ou não crie .env) para usar o proxy do Vite.
# Evite `localhost` no Windows — o Chrome pode resetar a conexão (IPv6).
# VITE_API_URL=http://127.0.0.1:3000
VITE_API_URL=
